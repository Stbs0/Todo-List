export default class CreateToDo {
  constructor(title, description, dueDate, priority, notes, checklist) {
    this.title = title;
    this.title = title;
    this.title = title;
    this.title = title;
    this.title = title;
    this.title = title;
  }
}
