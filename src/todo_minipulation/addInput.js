import ToDo, { CheckList, Note, Project } from "./create";

export default function updateScreen() {
  function createProject(name) {}
  function addProject(todo) {
    project.push(todo);
  }
  return;
}
